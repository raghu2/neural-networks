Libraries Used:
	numpy(np)
	matplotlib(plt)
	timeit
	math
Any known problems/errors:
	1.Accuracy=0
	  Error=100
	  even though my graph is being plotted according to the linearly separable data set(rounding off problem).
Environment of exectuion:
	Operating System:Windows 10
	RAM:12 GB
	IDE used:Pycharm by Jetbrains
Process:
	1.Creation of a Python file:
		On the top-right corner of the IDE, select "File" option and then select "New File" option which creates a Pure Python file.
	2.Debugging/Running the script:
		On the tool bar menu, click on Run(Alt+Shift+F10) and then the console displays the output if any error or the value is obtained.

